# Environment variables for Sanctions Screening API
# Copy this file to .env and modify as needed

# Server Configuration
API_HOST=127.0.0.1
API_PORT=8000

# Data Configuration
DATA_DIR=sanctions_data
CONFIG_PATH=config.yaml

# Security Configuration
MAX_UPLOAD_SIZE_MB=10

# API Key Authentication (required for production)
# Set this to enable API key authentication on protected endpoints
# Leave empty or unset to disable authentication (development mode)
API_KEY=

# CORS Configuration (comma-separated list of allowed origins)
# Example: CORS_ORIGINS=http://localhost:3000,http://localhost:5173
# Leave empty to use default localhost origins
CORS_ORIGINS=

# Logging (optional)
# LOG_LEVEL=INFO
