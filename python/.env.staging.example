# Environment variables for SDNCheck Staging Environment
# Copy this file to .env.staging and modify as needed

# ============================================
# SERVER CONFIGURATION
# ============================================
API_HOST=0.0.0.0
API_PORT=8000
ENVIRONMENT=staging
LOG_LEVEL=DEBUG

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Database connection (individual settings)
# ⚠️ SECURITY: Change these passwords before deploying to actual staging!
DB_HOST=localhost
DB_PORT=5433
DB_NAME=sdn_database_staging
DB_USER=sdn_user
DB_PASSWORD=CHANGE_THIS_STAGING_PASSWORD

# Alternative: Full database URL
# DATABASE_URL=postgresql://sdn_user:sdn_staging_password@localhost:5433/sdn_database_staging

# Connection pool settings (smaller for staging)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=1800

# ============================================
# SECURITY CONFIGURATION
# ============================================
MAX_UPLOAD_SIZE_MB=10

# API Key Authentication
# ⚠️ SECURITY: Generate a secure API key before deploying!
# Use: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=CHANGE_THIS_STAGING_API_KEY

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:8001

# ============================================
# REDIS CONFIGURATION (optional)
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_DB=0

# ============================================
# MONITORING CONFIGURATION
# ============================================
PROMETHEUS_PORT=9091
GRAFANA_PORT=3001
GRAFANA_PASSWORD=admin_staging

# ============================================
# DATA CONFIGURATION
# ============================================
DATA_DIR=sanctions_data
CONFIG_PATH=config.yaml
