# Railway Backend Environment Variables
# Copy this file and set values in Railway dashboard

# Port (automatically provided by Railway)
PORT=8000

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Enable database mode to use PostgreSQL instead of XML files
# When enabled, screening queries are executed against PostgreSQL
USE_DATABASE=true

# Database (use DATABASE_URL from Railway PostgreSQL plugin)
# Railway provides DATABASE_URL automatically when you add PostgreSQL plugin
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Alternative: Individual DB settings (if not using DATABASE_URL)
DB_HOST=your-railway-postgres-host
DB_PORT=5432
DB_NAME=railway
DB_USER=postgres
DB_PASSWORD=your-password

# ===========================================
# API CONFIGURATION
# ===========================================
API_HOST=0.0.0.0

# CORS - Set to your Railway frontend URL
# Supports wildcards for Railway subdomains: https://*.up.railway.app
CORS_ORIGINS=https://your-frontend.up.railway.app,https://*.up.railway.app

# Optional: API Key for protected endpoints
# API_KEY=your-secure-api-key

# ===========================================
# DATA CONFIGURATION
# ===========================================
# Data directory for XML files (used in XML mode only)
DATA_DIR=sanctions_data
CONFIG_PATH=config.yaml
